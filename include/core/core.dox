/// @defgroup core Core
/// @brief Application lifecycle, view hierarchy, event dispatch, and display.
///
/// The core module provides the fundamental building blocks: Application (run loop and event dispatch), Window (root view and display bridge), View (base class for all UI elements), ViewController (view lifecycle management), and supporting infrastructure like timers, tasks, and notifications.
